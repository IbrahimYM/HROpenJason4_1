<UserControl
    x:Class="MCSI.UWP.HROpen.Controls.AddressesCTRL"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MCSI.UWP.HROpen.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="350"
    d:DesignWidth="720">

    <Grid Margin="5" >

        <RelativePanel >

            <ListView Name="AddressList" Background="AliceBlue" HorizontalAlignment="Stretch" Width="400" BorderBrush="Black" BorderThickness="1" MinHeight="300"></ListView>
            <ContentControl Name="SelectedAddress" RelativePanel.RightOf="AddressList" Content="{Binding SelectedAddress}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"></ContentControl>
            
        </RelativePanel>

    </Grid>
</UserControl>
